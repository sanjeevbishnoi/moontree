/// infrastructure is the bottom layer of the domain architecture. It doesn't
/// actually have database logic or anything in it, instead it describes how to
/// pull from the datbase or any other source. it is organized in to 
/// repositories representing and as defined by the usecase domain concepts.